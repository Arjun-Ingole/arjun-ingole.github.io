# Arjun Ingole

Personal site built with Astro.

## Development

```sh
npm install
npm run dev
```

## Cloudflare Pages

- Build command: `npm run build`
- Build output directory: `dist`
- Node.js: `22.12.0` or newer

The site is fully static, so no Cloudflare adapter is required.
